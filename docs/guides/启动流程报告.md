# 启动流程报告（已废弃）

> ⚠ **本文档已废弃**（2026-06-10 旧版）。其内容与项目根 `STARTUP.md` 存在分叉与矛盾（如 Java 服务用 `java -jar`、AI 用 `0.0.0.0`、缺 Landing 入口、Admin 启动散落于此）。

**请以项目根 [`STARTUP.md`](../../STARTUP.md) 为唯一权威启动文档。**

`STARTUP.md` 为 2026-08-01 全栈启动实测定稿，含：
- 完整服务清单（含 Admin 3001 / Landing 5173 聚合入口）
- 真实可用启动命令（`mvn spring-boot:run` / `uvicorn --host 127.0.0.1`）
- 服务依赖关系图 + 网络架构
- 10 项故障排除（含 Kong 路由冲突、file-service DNS panic、`4ca6e09` 误删恢复等实测问题）
